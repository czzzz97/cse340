<% if (title) { %>
    <h1><%= title %></h1>
<% } else {
        res.redirect('/')
} %><% if (message) { %>
    <p class="notice"><%= message %></p>
<% } %>
<% if(data){ %>
    <%- details %>
<% } else { %>
    <p class="notice">Sorry, no matching vehicle could be found.</p>
<% } %>